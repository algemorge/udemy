/*************************************** ARRAY *******************************************/
// 배열 : 깂의집합, 값의 순서있는 집합.
// 약통처럼 칸이 나뉘고 순서대로 들어있다.
// JS에서 배열 생성하기 : 대괄호 사용 []
// length를 구할 수 있음
// JS에서는 배열이 여러개 타입으로 구성될 수 있음 ['HI', true, 101, null, NaN]

// 배열 생성 : 등호 오른쪽에 대괄호 -> 비어있던 배열에 변수가 부여된다.
let students = [];
let colors = ['red', 'orange', 'blue'];
let lottoNums = [19, 25, 2, 6, 7];
let stuff = [true, 67, 'eat', null];

// 문자열 안의 글자를 구할 때 사용했음
let bassist = 'Paul';
bassist[0]; //'P'

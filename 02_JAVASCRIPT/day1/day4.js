// booleans : true or false -> 예약어 (js에서는 소문자)
// 예) 게임이 실행중인가? 캐릭터가 죽었나? 이겼는지 졌는지? 

let isLoggedin = false;

let isGameOver = 1;

// js에서는 변수에 저장한 값의 타입을 변경할 수 있음

// typescript : js의 개량. 이런 타입 제한을 적용해줌. 
// 어떤 변수를 숫자 타입으로 지정하면 불리언이나 다른 변수가 아닌 숫자만 저장할 수 있게 해줌
// js에는 그런 제한이 없다.